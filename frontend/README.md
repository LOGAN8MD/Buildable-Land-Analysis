# Buildable Land Analysis Frontend

React and MapLibre client for the Buildable Land Analysis API. Complete setup, architecture, data-source, and tradeoff documentation is in the [project README](../README.md).

```bash
npm ci
npm run dev
```

The backend must run at `http://localhost:8000`. Use `npm run lint` and `npm run build` before submission.
